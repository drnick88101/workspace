#include <iostream>

using namespace std;

int main()
{
    //a program to check a given grade
    //if the grade is greater than or equal to 60 the student passed
    int grade = 82;

    if (grade >= 60) {
        cout<<"The student passed"<<endl;
    }
    return 0;
}
